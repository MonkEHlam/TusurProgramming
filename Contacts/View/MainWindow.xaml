<Window x:Class="View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:View"
        xmlns:vm="clr-namespace:View.ViewModel"
        mc:Ignorable="d"
        Title="MainWindow" Height="440" Width="609" MinWidth="650" MinHeight="468">

    <Window.DataContext>
        <vm:MainVM />
    </Window.DataContext>

    <DockPanel Margin="15">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="3"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Margin="5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Label Content="Find:" Margin="3" VerticalAlignment="Center"/>
                    <TextBox x:Name="SearchTextBox" Grid.Column="1" Margin="3" VerticalAlignment="Center"/>
                </Grid>

                <ListBox x:Name="ContactsListBox" Grid.Row="1" Margin="5"
                         ItemsSource="{Binding Contacts}"
                         SelectedItem="{Binding SelectedContact, Mode=TwoWay}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}" />
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="10,8,10,8">
                    <Button Content="Add" Width="80" Height="25" Margin="3"
                            Command="{Binding AddCommand}"/>
                    <Button Content="Edit" Width="80" Height="25" Margin="3"
                            Command="{Binding EditCommand}"
                            IsEnabled="{Binding IsContactSelected}"/>
                    <Button Content="Remove" Width="80" Height="25" Margin="3"
                            Command="{Binding RemoveCommand}"
                            IsEnabled="{Binding IsContactSelected}" HorizontalAlignment="Right"/>
                </StackPanel>
            </Grid>

            <GridSplitter Grid.Column="1" Width="3" HorizontalAlignment="Stretch"/>

            <Grid Grid.Column="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Width="100" Height="100" Background="LightGray" Margin="8,8,8,3" VerticalAlignment="Top"/>

                <StackPanel Grid.Column="1" Margin="8,8,8,8">
                    <Label Content="Name:" Margin="0,3,0,3"/>
                    <TextBox Text="{Binding SelectedContact.Name, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Margin="0,3,0,3" IsReadOnly="{Binding IsContactReadOnly}"/>
                    <Label Content="Phone Number:" Margin="0,3,0,3"/>
                    <TextBox Text="{Binding SelectedContact.Phone, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Margin="0,3,0,3" IsReadOnly="{Binding IsContactReadOnly}"/>
                    <Label Content="Email:" Margin="0,3,0,3"/>
                    <TextBox Text="{Binding SelectedContact.Email, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" Margin="0,3,0,3" IsReadOnly="{Binding IsContactReadOnly}"/>
                </StackPanel>

                <Button Grid.Column="1" Content="Apply" Width="75" Height="25" Margin="8,50,8,8" 
                        HorizontalAlignment="Right" Visibility="{Binding ApplyButtonVisibility}" Command="{Binding ApplyCommand}"/>
            </Grid>
        </Grid>
    </DockPanel>
</Window>
